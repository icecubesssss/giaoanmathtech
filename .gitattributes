# Tự động chuẩn hóa ký tự xuống dòng (LF cho macOS/Linux và tự chuyển phù hợp cho Windows)
* text=auto

# Trang web lưu về từ trình duyệt ("Save page as, complete" → đẻ ra thư mục `*_files/`).
# Đây là TƯ LIỆU DẠY HỌC, không phải mã nguồn dự án. Không khai báo thì GitHub tính
# 737 KB JavaScript nhúng của 1 trang GeoGebra là "HTML", đẩy nó lên 64,6% ngôn ngữ repo
# trong khi toàn bộ engine Python chỉ có 340 KB.
**/*_files/** linguist-vendored
