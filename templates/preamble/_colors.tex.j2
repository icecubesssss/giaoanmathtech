% Bảng màu — sinh từ config/design_tokens.json (KHÔNG sửa tay hex ở đây)
\usepackage{xcolor}
((* for name, hex in colors.items() *))
\definecolor{((( name )))}{HTML}{((( hex )))}
((* endfor *))
\color{ink}
