((= Chặng 5 — Tổng kết (hộp chặng) + 2 MỤC PHỤ tách riêng: BTVN, Mở rộng. =))
((* import "_blocks.j2" as B with context *))
((* set parts = split_reflection(stage.blocks) *))
((= Sơ đồ tư duy KHÔNG tự bẻ trang được: thiếu chỗ thì hộp chặng in ra một khung
    xanh RỖNG ở cuối trang rồi sơ đồ nhảy sang trang sau. Đòi đủ chỗ trước. =))
((* if parts.tong_ket | selectattr("type", "equalto", "mindmap") | list *))\stageneedspace{0.66\textheight}
((* else *))\stageneedspace{7.5cm}
((* endif *))
\begin{stage}{((( stage.number )))}{((( stage.title )))}{stage5}{\iconreflect}
\justifying\setlength{\parindent}{0pt}
((( B.render_blocks(parts.tong_ket) )))
\end{stage}
((* if parts.btvn *))
\worksheetsection{BÀI TẬP VỀ NHÀ}
\justifying\setlength{\parindent}{0pt}
((( B.render_blocks(parts.btvn) )))
((* endif *))
((* if parts.mo_rong *))
\worksheetsection{BÀI TẬP MỞ RỘNG (tự chọn)}
\justifying\setlength{\parindent}{0pt}
((( B.render_blocks(parts.mo_rong) )))
((* endif *))
