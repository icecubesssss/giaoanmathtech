# Sao chép file này thành .env và điền key thật. KHÔNG commit .env.
MATHPIX_APP_ID=
MATHPIX_APP_KEY=
LLM_API_KEY=
# Đường dẫn tới binary tectonic (engine XeLaTeX, không cần root)
TECTONIC_BIN=~/bin/tectonic
